'use strict'

const Replicators = require('../../src/replicators')
const testUtils = require('../utils')

describe('replicators-bulk', () => {
  // let replicators = null
  // let docIds = []
  //
  // const createReplicators = async () => {
  //   await testUtils.spiegel._slouch.doc.create(testUtils.spiegel._dbName, { db_name:  })
  // }
  //
  // beforeEach(async () => {
  //   replicators = new Replicators(testUtils.spiegel)
  // })
  //
  // afterEach(async () => {
  //   await Promise.all(
  //     docIds.map(async docId => {
  //       await testUtils.spiegel._slouch.doc.getAndDestroy(testUtils.spiegel._dbName, docId)
  //     })
  //   )
  // })
  //
  // it('should dirty if cleaned or locked', function () {})
})
